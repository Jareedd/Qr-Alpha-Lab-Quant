# Research Log — qr-alpha-lab

**Global trial count (feeds `--n-trials` for the DSR): N = 11**

Rules (from CLAUDE.md law #3): every strategy variant, hyperparameter tweak,
feature set, or horizon evaluated on **real data** gets one row and increments N.
N never resets. Synthetic planted/noise runs are harness validation, not
alpha-seeking trials — they are logged below but do not increment N.
Infrastructure changes are logged with a falsification-gate re-run (law #2).

| # | Date | Type | Hypothesis / change | Config | OOS result | Conclusion |
|---|------|------|---------------------|--------|------------|------------|
| — | 2026-06-10 | infra | Vectorize hot paths (IC, weights, walk-forward slicing) without changing results | ridge, planted/noise, defaults | planted: IC 0.0629, net SR 0.8646, DSR 0.9919 → PASS; noise: DSR 0.0004 → rejected. **All metrics byte-identical to pre-optimization baseline.** Core compute 2.25s → 0.51s (4.4×). | Optimization is behavior-preserving; pinned by 3 new equivalence tests against naive reference implementations. |
| — | 2026-06-10 | infra | Add baselines (law #5): 12-1 momentum decile L/S + equal-weight 1/N, same OOS dates, same backtester, same costs | quantile 0.1, monthly rebalance, 10 bps | On *planted* panel: momentum baseline net SR **1.19** vs ridge net SR **0.86** — the one-line baseline beats the ML model. On noise: momentum SR 0.19 (nothing, as expected). | Expected and instructive: the planted signal *is* 12-1 momentum, so ridge dilutes it across 5 features. Lesson for real data: a multi-feature model is not automatically better than the strongest single feature. Reported via `beats_mom_baseline` in every metrics JSON. |
| — | 2026-06-10 | infra | CI must enforce the falsification gate, not just run unit tests | `--fail-if-dsr-below 0.95` (planted), `--fail-if-dsr-above 0.5` (noise) in GitHub Actions | Gate passes locally with exit code 0; non-zero exit on violation verified by flag logic. | Leakage introduced by any future commit fails CI loudly. |
| — | 2026-06-10 | infra | Cache-key bug: yfinance cache keyed on `len(tickers)` → two same-size universes silently collide | `data.py` | n/a (no real-data runs yet) | Fixed: key on MD5 of sorted ticker list. Found before it could corrupt a real-data result. |

| 1 | 2026-06-10 | **trial** | Default config on real data: do the 5 standard features carry any cross-sectional signal in a liquid US universe? | yfinance 57 names (2010→now, 3 dropped by 90% coverage filter), ridge α=10, 21d horizon, decile L/S, monthly rebal, 10 bps | OOS 3126d: IC 0.0333 (t=7.77*), net SR 0.82, gross SR 0.89, turnover 3.81×/yr, maxDD −29%, DSR 0.998 @ N=1. Beats 12-1 momentum baseline (net SR 0.34); equal-weight long-only SR 1.00. | **Encouraging but NOT yet trustworthy.** (a) Universe = today's winners → survivorship bias inflates everything, esp. reversal ("buy the dip" works when every name is known to have survived); EW SR 1.0 shows the universe itself was a money printer. (b) *t-stat overstated: daily ICs of overlapping 21d labels are autocorrelated; ic.sem() assumes independence — needs Newey–West. (c) No sector/beta neutralization yet. Number goes in no write-up until PIT universe (Phase 2) + neutralization (Phase 3) are done. |
| — | 2026-06-10 | infra | Nested per-roll hyperparameter tuning must be possible without trial-count inflation | `--model ridge_cv`: inner expanding walk-forward inside each outer train window selects α from {1,10,100,1000} by mean inner IC; outer test never touched | Planted panel: ridge_cv IC 0.0626 / net SR 0.857 / DSR 0.991 vs fixed ridge IC 0.0629 / 0.865 / 0.992 — near-identical, as it should be. Gate re-passed (planted PASS, noise rejected). | Selection on training data only = in-sample model fitting, not a new trial. Leak-test pinned in test_tuning.py: corrupting all data after the train window cannot change the chosen α. 21 tests green. |

| 2 | 2026-06-10 | **trial** | Does trial #1's signal survive a survivorship-bias-aware universe? | Point-in-time S&P 500 (Wikipedia changes table walked backward): 810 members ever 2010→now, 661 priceable (81.6%), 149 dead names unpriceable. Same ridge defaults, `--n-trials 2`. | OOS 3378d: IC 0.0052 (t_NW **0.54**), gross SR 0.18, **net SR −0.01**, DSR 0.29, turnover 7.26×/yr. Momentum baseline net SR 0.03; EW (member-masked) SR 0.81. | **Trial #1's alpha was survivorship bias.** IC fell 0.033→0.005 and net Sharpe 0.82→−0.01 on the honest universe — McLean–Pontiff reproduced in-house. This is the project's strongest exhibit, not a setback. Residual bias remains (149 unpriceable dead names + missing delisting returns), quantified in `results/sp500_pit_coverage.json`. |
| — | 2026-06-10 | infra | Bug caught BY a baseline: PIT run showed equal-weight SR 3.34 — impossible (RSP ≈ 0.9), so something was broken | `pct_change()` default pad-fill fabricated frozen 0% daily returns for delisted names, crushing measured vol | After `fill_method=None` everywhere (features, backtest, baselines): EW SR 0.81 (sane); strategy results ~unchanged (net SR −0.03→−0.01), so the bug was isolated to return-series construction for dead names. Gate re-passed; regression test added (dead names must drop out of averages, never contribute phantom zeros). | A baseline so good it must be wrong is as informative as a strategy that looks too good. Law #5 paid for itself the first week it existed. |
| — | 2026-06-10 | infra | Newey–West IC t-stats (overlapping 21d labels autocorrelate daily ICs) | `metrics.newey_west_tstat`, lags = horizon, Bartlett kernel | Planted panel: t_naive 7.76 → t_NW 2.00 (≈√21 shrinkage, as theory predicts). Trial #2: t_naive 1.70 → t_NW 0.54. | Naive IC t-stats on overlapping labels overstate significance ~4×. All quoted t-stats are NW from now on. |

| 3 | 2026-06-10 | **trial** | Phase 3 question: is real signal hiding under factor exposure in the flat PIT result? | Trial #2 config + `--neutralize both` (sector demean over 12 GICS-as-of-today buckets incl. UNKNOWN; weights projected to zero ex-ante beta, rolling 252d past-only betas), `--n-trials 3` | OOS 3378d: IC unchanged 0.0052 (neutralization acts after prediction), gross SR −0.09, net SR −0.38, DSR 0.01. Vol fell 10.0%→6.5% (factor exposure removed), ex-ante β 0.009, realized β mean 0.05 (p95 0.23 — estimation drift between ex-ante and realized is real and now measured). Turnover 7.39×/yr unchanged; costs unchanged on a smaller vol base → worse net SR. | **Nothing was hiding.** The 5 standard features have no edge on the honest universe, raw or neutralized — factor exposure was *adding* (noisy) return, not masking alpha. Clean negative result for the write-up. Phase 3 machinery validated separately on planted panel: p95 |β| 0.32→0.03 with the planted idiosyncratic signal intact. Phase 4 must find better features/labels, not better risk plumbing. |

| 4 | 2026-06-10 | **trial** | Turnover attack: do the features survive at a slower cadence (63d horizon + 63d rebalance → ~3× less turnover)? | PIT sp500, ridge, h63/r63, neutralize both, member-masked z-scores, `--n-trials 4` | IC **−0.0278** (t_NW −1.95), gross SR −0.24, net SR −0.35, DSR 0.01, turnover 2.43×/yr | Turnover fixed, edge still absent — and the IC flipped *negative* at the quarterly horizon (t_NW −1.95, sub-threshold). **Recording the trap explicitly: we will NOT trade the sign-flip.** "My signal reversed works" after testing N variants is textbook max-of-N mining; if quarterly reversal is a real hypothesis it needs its own pre-registered test on fresh data, not a salvage of this one. |
| 5 | 2026-06-10 | **trial** | Residualized labels: does predicting idiosyncratic return (the only thing a neutral book harvests) beat predicting raw return? | PIT sp500, ridge, h21, `--label residual` (past-only rolling betas), neutralize both, `--n-trials 5` | IC vs residual label **+0.0225** (t_NW +1.91), but gross SR **−0.61**, net −0.77, DSR ≈ 0, turnover 3.46× | Most instructive failure yet: measurable-ish predictability of the residual label that does NOT monetize as portfolio return — the decile construction loses money gross while "IC" improves. IC against a cleaner label is partly definitional, and sub-2 t with N=5 is not evidence. Lesson: IC and P&L are different objects; report both, trust the P&L. |
| 6 | 2026-06-10 | **trial** | Nonlinear interactions: does GBR find structure ridge can't in the residual-label setup? | PIT sp500, gbr (depth 3, 200 iters), `--label residual`, neutralize both, `--n-trials 6` | IC +0.0077 (t_NW +0.80), gross SR +0.19, net SR −0.12, DSR 0.04, turnover 5.44× | No. Costs eat the thin gross edge; nothing statistically defensible. |

| 7 | 2026-06-10 | **trial** | Model-class ablation closer: does a small neural net find structure ridge/GBR can't? | PIT sp500, mlp (16,8) w/ scaler-in-pipeline + early stopping on train only, `--label residual`, neutralize both, `--n-trials 7` | IC +0.0093 (t_NW +1.21), gross SR +0.09, net SR −0.28, DSR 0.008, turnover 5.77× | No. The ablation is complete: linear (ridge, #5), trees (gbr, #6), shallow net (mlp, #7) on identical features and identical harness — all null. Pre-run validation: MLP recovers the planted signal (weaker than ridge, the Gu–Kelly–Xiu shallow-net result) and rejects noise (DSR 0.005), so the null is about the features, not the model or the harness. "No model class rescues information-free features" is now an evidenced sentence for the write-up. |

| — | 2026-06-10 | infra | Phase 5 on the honest universe: capacity sweep of the DEPLOYED config (trial #5: residual label, sector+beta neutral) — previously capacity existed only for the biased yfinance config. Same strategy under execution scenarios = not a new trial (`--capacity` changes nothing about alpha). | PIT sp500, ridge, residual label, neutralize both, sqrt impact k=1.0, spread 10 bps, ADV coverage 87.6%, `--n-trials 7` | Cost drag by AUM: $1M → 1.36%/yr, $10M → 2.27%, $100M → 5.16%, $1B → 14.30%. Gross edge is negative, so "capacity" is formally $0; the informative object is the drag curve: any future strategy with this turnover profile (3.46×/yr) needs ≥1.4%/yr of true gross alpha just to exist at $1M, ≥5%/yr at $100M. Bonus reproducibility exhibit: fresh data download + fresh venv + different CPU reproduced trial #5 to ~1e-7 relative (residual drift = yfinance retroactive dividend/split re-adjustment — measured, named, and a write-up footnote on why "point-in-time data" includes the *data revision* dimension). | Write-up §7 is no longer a TODO. Trial #5's original artifacts restored (the re-run was for capacity, not to re-document the trial); only `capacity_sp500_ridge_both_residlabel.json` is new. |
| — | 2026-06-10 | infra | Dependency hole that would have killed tomorrow's first unattended cloud cycle: `lxml` (pandas' `read_html` backend for the PIT universe scrape) was installed ad hoc on the laptop but absent from `requirements.txt` — `live.yml` installs ONLY requirements.txt, so the 2026-06-11 CI cron would have crashed at `fetch_sp500_tables` before sending any order | found when the fresh PC venv (a perfect simulation of the CI runner) died on the capacity re-run | Added `lxml>=4.9` to requirements.txt + a network-free test pinning the `read_html` backend (the suite never exercised it — exactly how it slipped through). Also: `live.yml` now regenerates the monitoring report after each cycle (non-fatal step) and commits it with the prediction log. | A fresh environment is the only honest test of requirements.txt. The machine migration accidentally audited deployment reproducibility before the cloud run could fail in production. |

## Phase 4 verdict (the central finding of the project so far)

Six trials. Five price-only features. On the survivorship-bias-corrected universe,
**no configuration produced a defensible edge**: best DSR 0.04, no |t_NW| ≥ 2 in the
right direction. This is consistent with the published record — large-cap US equities
are the most heavily arbitraged segment on earth, and price-only features there are
the most picked-over signals in history (McLean–Pontiff decay, Novy-Marx–Velikov cost
mortality). The pipeline is not broken: it recovers planted signals and rejects noise
on demand. The signals are just not there at this data quality. Phase 5–6 proceed with
the machinery (capacity, live paper trading infrastructure) demonstrated honestly on
what exists; new alpha hypotheses (fundamentals, alternative data, different universes)
are future work with their own pre-registered trials.

| — | 2026-06-10 | infra | Live-IC gap: `run_daily` logged decile *weights* but not *predictions* — live IC vs backtest IC (the point of Phase 6) needs the full prediction cross-section, and the backtest's `mean_rank_ic` is computed on PRE-sector-demean predictions (run_pipeline.py computes IC before `neutralize_predictions_by_sector`), so weights alone cannot reproduce it | `live.py` now logs `predictions_{asof}.csv` (cols `pred_raw` + `pred_sector_neutral`, full scored cross-section) before weights/orders. New `monitor.py` + `scripts/live_report.py`: cycle-continuity check (missed crons caught in days), per-cycle live IC vs realized residual label via the same `features.build_labels` machinery (Spearman-as-Pearson-of-ranks, identical tie handling to `models.information_coefficient`), mark-to-market of logged books as broker cross-check. Monitor is read-only by design — cannot feed back into the strategy. | 58 tests green (9 new known-answer tests incl. perfect-foresight IC = ±1.0 exactly, immature-cycle omission, book-holds-until-next-log). Also: per-date live logs are now **write-once** (`assert_write_once`) — a same-day re-run/CI retry/stray `--dry-run` would previously have silently replaced the "immutable" record; `--allow-overwrite` is the explicit escape hatch. Falsification gate re-passed; planted/noise metrics match the laptop baseline to reported precision (float drift ~1e-14 from different CPU/BLAS — measured, not assumed). Report runs end-to-end on the real day-1 log and honestly reports "0 measurable cycles". | Cost of the gap: cycle #1 (2026-06-10) has weights only — the live-IC record is permanently one cycle shorter than the trading record; recorded in README limitations. Every cycle from #2 onward yields IC once its 21d horizon matures (~2026-07-09). The t_NW needs >23 matured cycles before any live-vs-backtest claim is allowed. |
| — | 2026-06-10 | infra | Phase 6 live paper trading deployed: the live cycle must inherit the backtest's leakage discipline, not approximate it | `live.py` + `scripts/live_trade.py`: daily cycle trains ONLY on fully-labeled history (rows newer than today−horizon excluded); predictions logged to `results/live/` BEFORE any order exists; integer-share orders capped at 5% of equity per name; Alpaca client refuses any endpoint without "paper" in it. `live.yml` cron 22:30 UTC weekdays commits the prediction log back to the repo as an immutable record. | First live cycle ran end-to-end 2026-06-10: 100-name book (50L/50S) from ~500 PIT members, 100/100 orders accepted, zero failures, queued for the 2026-06-11 09:30 ET open — reproducing the backtest convention (weights at t earn from t+1) by construction. 49 tests green incl. order-delta known answers, per-name cap, paper-only guard, and complete-label training checks. | Live IC vs backtest IC becomes measurable as each 21d horizon elapses; the committed prediction logs are the tamper-proof record. CI skips gracefully until Alpaca secrets are configured in the repo — **until then, the daily cycle only runs from a machine holding `.env`**. |
| — | 2026-06-11 | infra | The live experiment needs a CONTROL ARM: if live IC sags vs backtest, "model decayed" and "period hostile to everything" are indistinguishable without a same-period reference signal | `live.py` shadow-logs `baseline_mom_12_1` (the law-#5 momentum baseline, z-scored = rank-identical) in every `predictions_*.csv`; no orders ever sent for it. `monitor`/`live_report` compute its live IC alongside the model's; `run_pipeline` now emits `baseline_mom_ic` (+ NW t) in every metrics JSON as the backtest-side anchor (planted: mom IC 0.0753 vs ridge 0.0629 — the planted signal IS momentum, as known since the baselines row). | Pinned by a test that the logged column equals `build_features()["mom_12_1"]` name-for-name on the as-of date. Cost of the late start: cycles #1–2 have no control column; the control-arm IC record starts 2026-06-11 and is dated in README limitations. | A live test with a control is a controlled experiment; without one it's an anecdote. Zero marginal trades, zero trial-count cost (measurement only, never feeds back). |
| — | 2026-06-11 | infra | Data-revision monitor: the capacity re-run showed yfinance retroactively re-adjusts history (~1e-7 drift); the live cron's per-day price caches make that drift MEASURABLE daily instead of anecdotal | New `revisions.py`: each cycle diffs today's downloaded panel vs the previous cycle's snapshot of the SAME past; price-cell changes (benign whole-history re-scalings) counted separately from RETURN-cell changes (the kind that silently alter features/labels — they cluster at adjustment splice points). Per-cycle `revisions_*.json` committed with the live record; `scripts/data_revisions_report.py` aggregates; report section in `live_report`. Wrapped non-fatally: a measurement bug can never cost a trading cycle. Also fixed pre-existing Windows-only crash (cp1252 stdout) in `live_report.py` that reported exit 1 after writing the report successfully. | Known-answer tests: full-history rescale → N price cells, 0 return cells; splice rewrite → exactly 1 return cell; only the shared past is compared (a new trading day is information, not revision). First comparable pair lands one day after the first post-change cycle. | "Point-in-time" has a data-VALUES dimension, not just a universe dimension: backtest and live model literally train on different versions of the same past, and now the project measures by how much instead of footnoting it. |
| — | 2026-06-11 | infra | Missing delisting returns: BOUND the bias, never impute it (delistings are MNAR — missing *because* the company died; any imputation fit on surviving names re-injects survivorship bias by construction, law #7) | `synthetic.inject_delisting_returns` (scenario tool, lives in synthetic.py, loudly labeled) + `run_pipeline --delisting-return`: forces one synthetic final print on every name whose series ends ≥5 trading days before the panel ends; artifacts tagged `_dlret±NN` with a scenario stamp inside the JSON. Trial #2 config (PIT sp500, ridge, raw label), paired runs in one environment: 0% control vs −30% Shumway worst case, `--n-trials 7`. | 29 of 661 priceable names actually die mid-window (most index-removed names keep trading). 0%: IC 0.0028, net SR +0.008; −30%: IC 0.0028, net SR +0.014. The pure delisting-return effect is **ΔSR ≈ +0.006** (book was on average slightly short the dying names) — the missing final prints can neither rescue nor sink the null. NOT a new trial: same strategy under a data assumption, N stays 7. Both scenario runs differ slightly from logged trial #2 (IC 0.0052 → 0.0028) — environment drift (pandas 3 + fresh data download), which is why the bound is read from the PAIR, not against the old row. | The honest answer to "can't ML fill in the missing data?" is this row: bounding beats imputing. Residual unbounded hole = the 149 names with NO price history at all; bounding those needs paid data (CRSP delisting returns), now a precise statement in the write-up instead of a vague caveat. |

| — | 2026-06-12 | infra | Regime machinery, falsification-first (the Simons/HMM idea done in the only defensible order): build the detector, prove it cannot see the future, prove the standard construction CAN, and only then consider real data | New `regime.py`: 2-state Gaussian HMM, numpy EM, no new deps. Exposes causal `filtered_probs` (the only strategy-legal output) AND anticausal `smoothed_probs` (what hmmlearn-style libraries return) so the leak is demonstrable. `causal_regime_probs` = expanding past-only refits + forward filter. New synthetic mode `planted_regime`: momentum signal exists ONLY in a persistent calm state (hidden chain, ~100d duration, stressed = 2.5x mkt vol, ground truth in attrs); planted/noise draw sequences untouched (pinned by hardcoded golden values at n_days>lookback + gate re-run: planted IC 0.0629 / SR 0.8646 / DSR 0.9919, noise rejected — exact match). 8 new tests: EM parameter recovery; perturb-the-future pins (filtered bit-identical under future corruption, smoothed moves AND flatters — smoothed state accuracy > filtered, the gap a naive backtest pockets); walk-forward causality (exact equality); paired-control signal verification; gate adds lift with signal (+0.018) and none without (−0.008); detector OOS accuracy 94.4%; hostile-input guards. | **Discovery worth the whole exercise:** absolute IC-level tests failed twice before the paired-control formulation, because residualized-label momentum diagnostics carry a vol-regime-DEPENDENT artifact even on signal-free data: ~+0.13 IC in stressed states with dispersed betas (beta-estimation error × 2.5x vol), still ~+0.06 with uniform betas (label machinery × regime interaction). On real data this would masquerade EXACTLY as "momentum works conditionally on volatility" — the H3/H4 hypothesis itself. Consequence: H4's success criteria now require a paired artifact control, registered before any run. N stays 7 — nothing alpha-seeking ran on real data; H4 (HMM gate) + H5 (revision-intensity dataset, two-stage) registered in preregistered_hypotheses.md. 77 tests green. | The leak everyone ships (smoothed HMM states) is now a demonstrated, regression-pinned exhibit instead of a warning in a comment — and the synthetic lab caught a confound that would have produced a false positive on the project's own registered hypothesis. The live config remains frozen mid-experiment; nothing here touches it. Pre-commit adversarial review (4 independent lenses, every finding re-verified by a hostile second pass) caught and fixed before landing: a hollow regression pin (compared planted to noise at 50d where they are identical by construction — could never fail; rewritten with golden literals), a tolerance-weakened causality assertion (now exact), a silent point-mass collapse that would have mislabeled a one-bad-print state as "calm" and zeroed an H4-style gate forever (scale-relative sigma floor + loud degeneracy guard + hostile-input tests), silent NaN paths on degenerate inputs (now raise), an overclaiming docstring, and a 0.1pp rounding slip in this very entry. Review is not optional even for the reviewer. |
| — | 2026-06-12 | infra | Stretch goal pulled forward with owner sign-off: a one-page READ-ONLY Streamlit dashboard over results/ + research_log.md (spec written first in dashboard/SPEC.md, build held to it) — the integrity machinery made visible: falsification-gate banner, live-experiment monitor (continuity, maturity countdown, control-arm-aware live-vs-backtest IC via quantlab.monitor, public-price marks), the trial ledger with the DSR-vs-N luck hurdle (quantlab.metrics.expected_max_sharpe, n_obs from the deployed config's artifact), data-revision series, capacity curve, README limitations verbatim | `dashboard/app.py` + pure parsers in `dashboard/loaders.py`; streamlit isolated in requirements-dashboard.txt (trading path untouchable by dashboard deps); no quantlab.live import; network only behind an explicit fetch button; every panel captions its source artifact | 84 tests green (14 new: known-answer parsers incl. the real log, AppTest smoke runs of the real page in both modes, a synthetic-mature-artifacts run exercising the calendar-activated branches before they ever run in production, and a producer/consumer contract pin for the fetch computation). src/quantlab/ untouched → no gate re-run required; gate artifacts re-read and green on the page itself. | Two real parser bugs found ONLY by testing against the real log, not the fixture: a sentence-ending period broke DSR extraction ('DSR 0.01.'), and trials 5–7's IC/turnover/net-SR silently rendered blank (formats: '+'-signed IC, bare 'net −0.77', 'turnover 3.46×' without /yr); a literal \| inside trial #3's conclusion silently truncated it. Pre-commit adversarial review (4 lenses, findings re-verified) confirmed 22 findings incl. those, a crash path on a corrupt revisions JSON, fabricated '0/0' orders for dry-run summaries, and two dropped spec promises (mark-to-market cross-check, NW t-stat display) — all fixed before landing. Lesson: a dashboard that renders the research record is itself research code; it gets the same hostile review or it quietly lies. |
| — | 2026-06-12 | infra | H2 pre-data design session (/research-session a): steelman the null, build the carry falsification world, finalize the trial-#8 registration — ZERO exchange data touched (law 5) | New `synthetic.make_perp_panel`: perp world with funding panel in attrs; the carry-specific insight encoded is that the NULL is not "no funding" but "funding fully PRICED" (`priced_carry`: mark drift offsets funding exactly; total returns unpredictable from funding) vs `planted_carry` (fraction γ=0.3 of funding is true premium). One-line carry book (short payers / long receivers, weekly, quartiles) as the killing test. H2 registration amended, dated, pre-data: funding-INCLUSIVE label, PIT universe incl. delisted perps from exchange dumps, 8h-settlement timestamp conventions, shuffled-funding paired control, costs pinned (5+2 bps/side + sqrt impact). Full design: `writeup/h2_carry_design.md`. | 95 tests green (3 new). Falsification gate re-passed byte-identical (planted IC 0.0629/SR 0.8646/DSR 0.9919; noise rejected 0.0778) — legacy worlds untouched, also pinned by the golden-value test. Label lesson DEMONSTRATED in-lab: the same planted-carry book scores SR ≈ +0.9…+1.6 funding-inclusive vs ≈ −1.9…−2.7 price-only — a price-only label would reject a TRUE premium. | Adversarial check (law 8) caught the first version of the carry tests being seed-fragile: the null world's absolute SR has se ≈ 0.4 at 6 synthetic years (measured +0.57 on one seed from pure luck) — re-pinned as PAIRED per-seed differentials (planted − priced ≈ +1.0…+1.2, stable), the third time this project has found absolute levels lying where paired controls don't. N stays 7; trial #8 is registered, designed, machinery-gated, and awaits owner sign-off + data download. |

| — | 2026-06-12 | infra | Edge ideation, diverge→attack→register: 20 candidates screened against the machine's own scars (survivorship meta-kill, cost mortality, borrow on the short leg, sign-flip salvage, paired-control doctrine) | `writeup/edge_candidates_2026-06-12.md` — owner-produced, committed as the ideation record. 17 of 20 removed on the record (15 killed, C1 parked behind H2's execution to avoid N-inflation, D4 protocol-blocked as H5). Three survive as registration-ready specs: H6 CEF tail-discount reversion (two-stage, Stage-1 census first), H7 borrow-fee snapshot collection (zero trials, unbackfillable-dataset moat), H8 S&P discretionary-deletion post-effective test vs matched control (weak prior declared, power gate ≥100 events). | No price data touched; N stays 7. The two recurring assassins were named as permanent ideation screens: free data's missing dead names, and borrow on the short leg — future ideas must live where nothing dies, die conservatively, or be collection-forward. | Killing 17 ideas on the record is itself a write-up asset: the selection pressure is the methodology. H7 signed off same day (see next row); H6/H8 remain registration-ready, unregistered. |

| — | 2026-06-12 | infra | Owner sign-off session: H7 registered AND deployed same day (the clock argument — every uncollected day is unbackfillable history lost), and law #3 mechanized into the pipeline | (1) H7: `borrow.py` + `scripts/collect_borrow.py` — daily IBKR short-availability snapshot of the live universe (endpoint VERIFIED at registration: ftp2.interactivebrokers.com/usa.txt, ~19.5k instruments, file carries its own #BOF timestamp), write-once `borrow_{asof}.json` committed with the live record, non-fatal live.yml step (revisions.py pattern — a collector bug never costs a cycle); registration entered in preregistered_hypotheses.md as COLLECTION-ONLY (no analysis before Stage 2 at ≥60 cycles). FIRST SNAPSHOT TAKEN 2026-06-12: 498/500 universe names, fee p50/p90/p99 = 1.2%/17.3%/163.7% — the fee tail is real and now in our record. (2) Law #3 mechanized: `registry.py` + `run_pipeline --hypothesis/--reproduce` — real-data runs now REFUSE to start unless they name a PROPOSED registration (spends a trial) or declare a reproduction; synthetic modes exempt (synthetic data is free by law). Verified end-to-end: flagless sp500 run refused; `--hypothesis H7` refused (collection-only ≠ runnable). | 101 tests green (6 new: IBKR format known-answers incl. '>' caps and malformed-line counting; registry parsing incl. prefix collisions H1-vs-H10; refusal paths incl. against the REAL registry). Falsification gate re-passed byte-identical (planted 0.9919 / noise 0.0778) — the gate itself now runs through the new flag plumbing, validating it. | Live trading path untouched (collector is a separate script + separate workflow step; live.py frozen). N stays 7 — H7 is collection-only by construction. Explicitly NOT done on the blanket approval: trial #8 — a trial spend requires deliberate sign-off against its registration, not approval-in-passing; the H2 run waits for the words "run trial #8". |

| — | 2026-06-13 | infra | First real data-revision measurement (cycle 2026-06-12 vs -13 snapshots) — and it immediately calibrated its own instrument | The raw fingerprint flagged 51.5% of 1.30M shared price cells and 60.7% of return cells "changed" at the original 1e-9 tolerance. Decomposition: (a) a ubiquitous ~1e-7 relative wobble — the vendor RE-SERVES history with float noise (the same drift the capacity re-run measured, now quantified at panel scale); (b) on top, unmistakably real events: full-history dividend re-scalings (FIS 1.1%, LUV 0.43%, CRM 0.26%) and one 90% split-factor/repair rewrite (KLAC, 2,059 return cells). Fix per the module's own law (refine the measurement, never the data): REL_TOL 1e-9 → 1e-6 (above the serving-noise floor, three orders below real events) with the (1e-9, 1e-6] band counted separately as `n_*_noise_band` — the noise phenomenon stays measured, never mistaken for revision. | 102 tests green (new known-answer: uniform 1e-7 wobble → all noise-band, zero changes, zero return changes). The miscalibrated first fingerprint stays committed as-is — it is the calibration record; fingerprints are write-once. Schema change is additive; dashboard unaffected. | Day one of measurement found both regimes at once: the vendor's serving precision is ~1e-7 (point-in-time data has a FLOAT dimension too), and real history rewrites up to 90% happen within a single day. Also: cycle 2026-06-12 traded clean (98 names, 89 orders, 0 failures); CI's borrow attempt was correctly write-once-refused against the same-day manual snapshot — CI owns collection from tomorrow. |

| — | 2026-06-13 | infra | Unattended session (owner away, standing constraints honored: no trial #8, live frozen, N=7, no new registrations): Phase-7 draft fact-checked against the post-draft record, and H8's zero-trial power gate decided | (1) `research_note_draft.md` updated where the record had moved past it: the delisting-bias claim ("pushes returns up") replaced with the MEASURED bound (ΔSR +0.006, book slightly short the dying names); §6 gains the regime confound + smoothed-HMM leak, the paired-controls doctrine (3 instances), and the revision-monitor day-one calibration; §8 gains the control arm, revision fingerprints, borrow collection, cycles 1–3 status; §10 notes law #3 is now machine-enforced. Owner's voice rewrite still pending — facts now current. (2) H8 census: `fetch_sp500_tables` retains Wikipedia's reason column (cache-invalidation handled); `classify_removal_reason` keyword classifier (corporate_action / distress / migration / discretionary / unknown — unknowns shown, never guessed); `scripts/h8_event_census.py` → `results/h8_event_census.json`. ZERO price data touched. | 326 removals since 2010 → 188 corporate actions, **124 discretionary**, 10 unknown (shown), 4 distress. **H8's power gate (≥100) PASSES on the keyword census.** Honest caveat, recorded: the census found ZERO 'migration' phrasings — Wikipedia's "market capitalization change" text likely hides MidCap migrations INSIDE the discretionary 124 (exactly the Greenwood–Sammon conflation H8's spec warns about), so 124 is an upper bound and the registered 20-event manual reconciliation must split it before any registration is signed. 108 tests green (12 new incl. real-phrasing pins from the first census run). Falsification gate re-passed (0.9919 / 0.0778). | H8 advances from "registration-ready, power unknown" to "registration-ready, power gate passed-with-caveat" at zero trial cost. N stays 7. |

| — | 2026-06-13 | infra | Trial #8 (H2) GREENLIT by owner; harness built, data download in flight — the run itself not yet executed, so this row is infra and N stays 7 until the result lands | Source verified (data.binance.vision reachable, 729 USDT perps ever listed incl. delisted LUNAUSDT → PIT universe with dead contracts is free, the registration's central claim confirmed). New `perp_data.py` (listing-bounded monthly downloader — one directory listing per symbol instead of 82 month-probes, after the naive version projected ~11h; caches per symbol, resumable; daily funding = sum of 8h settlements; delisting falls out of the data, e.g. LUNAUSDT correctly dies 2022-05-13). New `perp_carry.py` (PIT top-30-by-ADV universe, trailing-7d funding signal, funding-INCLUSIVE total-return label, dollar-neutral quartile book, 7bps/side, shuffled-funding paired control, funding/price P&L decomposition). New `scripts/run_carry.py` = the registration-gated trial entry point: refuses unless H2 is PROPOSED, runs the synthetic machinery gate (planted_carry recovered / priced_carry rejected, paired) in-env BEFORE the real run, then reports against the registered success+kill criteria — but does NOT auto-bump N or auto-log (spending the trial is a deliberate act after reading the result). | 114 tests green (6 new carry-harness known-answers: funding-inclusive returns, PIT universe, dollar-neutral correctly-signed book, shuffle control, forward-label timing, the machinery gate). Falsification gate re-passed byte-identical (planted 0.9919 / noise 0.0778). Fixed a latent loader bug before it could bite: Binance's older months are headerless and newer ones aren't — naive concat would silently shred any symbol spanning the boundary (exactly the long-history symbols the trial needs); now all months read positionally. | The run executes when the full-universe download completes; the machinery gate runs immediately before it; the result gets logged whatever it says and N becomes 8 at that point. Live config untouched throughout. |

| 8 | 2026-06-13 | **trial** | H2: does funding-rate carry in crypto perps clear our evidence bar, where price-only equity features did not? (owner-greenlit; registered config, run via the registration-gated `run_carry.py` after the synthetic machinery gate passed) | PIT top-30-by-ADV USDT perps from Binance public dumps (729 ever, incl. delisted; 2019-09→2026-05), trailing-7d funding signal, **funding-inclusive** total-return label, dollar-neutral weekly quartile L/S, 7bps/side, `--n-trials 8` | OOS 2342d: IC **−0.0369 (t_NW −3.54)** correctly signed, net SR **+0.87**, gross 0.93, **DSR 0.865**, turnover 35×/yr, **skew −1.87, maxDD −74%**. funding P&L +2.10 / price P&L +1.08 (both positive). Shuffled-funding control SR 0.08 (flat); survives ex-top3 (BTC/BNB/LINK) SR 0.68. **Registered criteria NOT MET** — only the DSR≥0.95 leg fails (0.865). | **The first non-null in the project — and the discipline still refused it.** Adversarial diagnostics (`results/h2_carry_diagnostics.json`): (a) entry-lag sweep decays gracefully 0.87→0.39 over 1–5d → real edge, NOT a timing leak (a leak collapses immediately); (b) only 2/28 dead names held at death (1 long/1 short) → no delisting optimism; (c) **regime split is the story: SR 2.28 in 2020–21, decaying to ~0.4 post-2021** as basis farms (Ethena-style) scaled — McLean–Pontiff decay caught live in a second asset class. The carry premium is REAL (validating that the 7 equity nulls were genuine absence, not pipeline impotence), but it fails our own pre-registered DSR bar at N=8 AND is crash-skewed — a textbook case of why Sharpe lies on negatively-skewed strategies. NO criteria relaxation (that is the salvage the project refuses). H2 status → RUN; N=8. |

| 9 | 2026-06-13 | **trial** | H8: do discretionary S&P 500 deletions earn positive post-effective drift vs a SIZE+MOMENTUM-matched control? (owner-greenlit; registered via `run_events.py` after the synthetic planted-event gate passed) | PIT membership change table 2010→now, reason-classified to 124 discretionary deletions (89 priceable, 75 usable after 126d-history + 60d-forward + matching); enter effective+1, hold 60d, long deleted vs matched basket (size = log trailing-63d $vol [PIT proxy for mcap, amendment dated 2026-06-13], momentum = trailing 126d ret), 10bps/side, `--n-trials 9` | daily event-time portfolio: net SR **−0.04**, **t_NW −0.10**, **DSR 0.05**. Event-level mean 60d excess +2.2% (simple t **0.87**, n.s.); **matched control itself rebounded +2.6%** over 60d. Subperiods: 2010–14 SR −0.85, 2015+ SR +0.13. **Registered criteria NOT MET** (every leg). | **Clean null — and the matched control is why it's honest.** Deleted names DO bounce (~+4.8% raw over 60d), but a size+momentum-matched basket of contemporaneous members bounces +2.6% of that — the rebound is small-loser mean reversion, NOT an index-deletion effect; the +2.2% residual is insignificant and negative pre-2015. Greenwood–Sammon's "disappeared deletion effect" reproduced in-house in the strictly-different post-effective window. The synthetic planted-event gate passed first (planted drift recovered SR 1.46, null rejected SR −0.55), so the null is the harness finding genuine absence, not impotence — the project's SECOND documented anomaly-replication-with-control (cf. trial #2 survivorship). Coverage caveat: 49 of 124 discretionary deletions unusable on free data (unpriceable or insufficient window) — CRSP would close it; the missing names can't bias a rebound we simply don't measure. NO entry/holding-period variants (salvage forbidden). H8 → RUN; N=9. |

| — | 2026-06-14 | infra | Graduation-candidate selection (/goal session): rank the open hypotheses by probability of *graduating* (clearing every pre-registered leg at once), with the DSR bar made concrete. ZERO trials, no registration, no real price data, N unchanged. | `scripts/graduation_hurdle.py` (pure arithmetic on `quantlab.metrics`, no market data) + analysis in `writeup/graduation_candidates_2026-06-14.md` | DSR≥0.95 hurdle at N=10 in net-annual-Sharpe units: **~0.88 @15yr (3378 obs) → ~1.06 @6yr (2342) → ~1.32 @6yr/1512 → ~2.28 @2yr (504)**; skew is second-order (carry-like −1.87 adds only ~3–5%). Reproduces trial #8 exactly (needed 1.09 at its sample, got 0.87 → DSR 0.865). Two drivers: sample length dominates (hurdle ∝ 1/√n_obs), skew is the nail not the coffin. **Top-3 by P(graduate): #1 H6 CEF tail-discount reversion** (structural no-arb premium, capacity-protected, low turnover, *conservative* survivorship direction — the only idea where missing dead names help; ~35–45% conditional on Stage-1 data depth); **#2 H1 quality fundamentals** (lowest bar — 15yr PIT + 63d rebalance → ~0.88 net SR, benign skew — but thin/arbitraged edge; ~25–35%); **#3 C1 long-tail perp carry** (proven mechanism from trial #8, but short sample → higher bar + worse skew + decay; ~15–25%, must be a fresh pre-registration on held-out data, not a #8 salvage). | Spend the next trials where the *structural* odds are best, not to manufacture a pass. Eliminated: H3/H4 (registered trap checks, weak prior), H5 (Stage-2 ~Sept 2026, prior is risk not alpha), H8 (now RUN as trial #9, null). All three picks' Stage-1 work is zero-trial; recommended order H6 census → H1 audit → C1 power analysis. **INTEGRITY FLAG (RESOLVED same day):** the note's trial #9 was real but *unmerged* — it lived on branch `h8-trial-9` (commit `634cd6c`: `events.py`, `run_events.py`, planted-event gate, `tests/test_events.py`, `results/metrics_h8_events.json`, plus the trial-#9 row now restored above) and was never merged to main, while `research_note.md` referencing it was committed straight to main. Reconciled by merging that branch: N→9, trial-#9 row restored, artifact matches the note (75 events, net SR −0.04, DSR 0.05). Law #3 + #8 gap closed. |

| 10 | 2026-06-14 | **trial** | H9: does funding carry survive in the liquid perp TAIL (ADV ranks 31–150) beneath the majors, where funding is far wider but fills worse? (C1 candidate; parking condition triggered by trial #8; fee-first power analysis passed FIRST — `scripts/carry_tail_power.py`: gross funding spread 71.8%/yr, 10–29× cost, ~8yr depth) | PIT tail (trailing-30d ADV ranks 31–150, min 20 names; 729 contracts ever incl. delisted, active 2020-09→2026-05), trailing-7d funding signal, **funding-inclusive** total-return label, dollar-neutral weekly quartile L/S, **20 bps/side** (5 taker + 15 spread, conservative for tail), `--n-trials 10` | OOS 2342d (~8yr, median 120 names/day): IC **−0.0240 (t_NW −3.62)** correctly signed & strongly significant, but net SR **−0.13** (gross 0.26), **DSR 0.024**, turnover 30×/yr, skew −0.43, maxDD −46%. **funding P&L +1.23 / price P&L −0.85** — price drift OFFSETS the funding (priced-carry). Shuffled-funding control SR −0.28 (~flat, passes); ex-top3 SR −0.18. Subperiods: 2020–21 SR +0.36, 2022+ SR −0.34. **Registered criteria NOT MET** (net SR<0 AND DSR fail). | **The tail carry signal is REAL but unmonetizable — the cleanest "IC ≠ P&L" exhibit in the project.** The funding signal predicts cross-sectionally as strongly as the majors (t_NW −3.62 vs trial #8's −3.54), but in the tail the price drift offsets the funding almost entirely (funding +1.23, price −0.85 → gross only +0.26 SR) and 20 bps tail costs push net NEGATIVE. This is the pre-declared "tail carry is fully priced / consumed by fills" outcome: where #8's majors carry netted +0.87 (failing only DSR), the tail nets −0.13. Decay confirmed (2020–21 +0.36 → 2022+ −0.34). Machinery gate passed (planted−priced differential 1.07–1.21) and the shuffled control is flat, so this is a genuine economic null, not harness impotence — and it teaches trial #5's lesson at scale: a t=−3.6 signal that loses money net. NO cost/rank-band/holding-period salvage (each is +1 trial). H9 → RUN; N=10. |

| — | 2026-06-14 | infra | H1 PIT DATA AUDIT — the registration's blocking prerequisite ("cannot run until the data question is answered honestly; that audit is its own session"). Can free SEC XBRL give survivorship-safe, filing-date-PIT fundamentals for GP/Assets + accruals/Assets on the S&P universe? ZERO trials — no forward returns computed. | `scripts/h1_fundamentals_audit.py` (SEC `frames` coverage CY2023∪CY2024 + `companyfacts` filing-lag; expanded legacy revenue/CoGS tags) | 872 PIT tickers, **only 73% map to a CIK** (SEC's `company_tickers.json` is current-only → dead/renamed names unmapped — the survivorship hole, again). Filing dates ARE in XBRL (Assets `filed`−`end` median 53d) → PIT lag by filing date is FEASIBLE. Coverage on mapped names: Assets 99%, NI 94%, CFO 97% → **accruals/A 93%**; but GrossProfit is tagged ~0% directly and GP=Rev−CoGS caps at **~59%** because financials/REITs (~40% of the index) have NO CoGS line (GP is a non-financials feature by construction, per Novy-Marx). Effective both-feature × survivorship-safe coverage ≈ 73%×53% ≈ **39%**. | **H1 is feasible to BUILD but NOT creditably RUN on free data today — and the blocker is survivorship, reprised.** The dead names that made trial #2's point are exactly the ones SEC's current-only ticker map drops; running GP/A quality on current-members-only would re-commit trial #1's original sin. A defensible H1 needs either (a) a historical ticker→CIK map for dead names, or (b) paid fundamentals (Sharadar/Compustat), PLUS a non-financials restriction for GP/A. Accruals/A alone is cleanly coverable (93%) and sector-agnostic — a candidate reduced registration. This is the honest answer to the registration's blocking question and the fundamental analogue of the project's survivorship exhibit. N stays 10 (zero-trial). |

| — | 2026-06-15 | infra | H6 Stage-1 census (the #1 graduation candidate, `graduation_candidates_2026-06-14.md` §3): can free CEFConnect data support the discount-z reversion test, and — the make-or-break — is the dead-fund bias direction conservative? ZERO trials, no signal-vs-forward-return computed. | New `src/quantlab/cef_data.py` (CEFConnect api/v3 layer — `funds`/`dailypricing`/`pricinghistory`, stdlib urllib + parquet cache, pure parsers split out) + `scripts/cef_stage1_census.py` → `results/cef_stage1_census.json`; `tests/test_cef_data.py` (7 known-answer parser tests). `--n-trials` untouched, N=10. | Snapshot as-of 2026-06-12: **362 current CEFs → 188 sub-$400M, 185 tradable (ADV≥$250k)**. Median discount −7.6%, **101 funds at <−10%** (wide-discount tail is real). **95% publish daily NAV (lag≤1d)** → staleness is NOT a broad killer; the daily-NAV-only filter keeps ~the whole universe. CEFConnect's `DiscountData` == our `cef.discount` (P−NAV)/NAV in % (cross-check pinned). **Depth is heterogeneous and WEEKLY:** free NAV/discount history is weekly back to ~2012 (some to 2003), DAILY only for the trailing ~1yr — and a meaningful slice of the tradable tail has thin served history (XFLT 10 weekly rows, EARN 0, EIC sparse), so Stage-2 must filter on *actual served depth*, not just current mcap/ADV. Daily PRICE is separately available full-span via yfinance. | **Data-depth + liquidity + staleness all PASS; the gate is NOT cleared.** CEFConnect's universe is current-listings-only → dead/merged/liquidated funds are absent, so the H6 thesis's load-bearing claim ("CEF deaths happen at NAV → omitting dead funds biases the backtest *conservatively*") is **UNVERIFIED**. **NEXT INCREMENT (make-or-break): dead-fund census from an INDEPENDENT source** (SEC N-CEN enumeration or a date-stamped historical universe) — settle the conservative-direction claim before any Stage-2 registration; if dead funds are absent AND that argument fails → kill H6 before a trial. Corrects the prior H6-machinery commit (`4fb777a`) claim of "~8yr daily NAV": the free *daily* NAV path is trailing-1yr only (multi-year is weekly). Falsification gate re-passed (planted recovered / noise rejected); 7 new tests green (135 total; 3 pre-existing dashboard failures are an env-only matplotlib 3.9.2 `print_png(width=)` mismatch, unrelated to this work). Live config untouched. |

| — | 2026-06-15 | infra | H6 Stage-1 **dead-fund census** — the make-or-break gate: are CEF deaths NAV events (so omitting dead funds biases a discount-long backtest CONSERVATIVELY)? Needs an INDEPENDENT source because CEFConnect is current-listings-only. ZERO trials. | New `src/quantlab/cef_deaths.py` (SEC EDGAR layer: EFTS form `25-NSE` delistings per year + per-filer submissions JSON; N-2 = closed-end/BDC filter; fully-delisted check excludes preferred/note redemptions of still-listed funds; filing-signature + name outcome classifier, unknowns shown not guessed) + `scripts/cef_dead_fund_census.py` → `results/cef_dead_fund_census.json`; `tests/test_cef_deaths.py` (10 known-answer tests). N=10. | Window 2021–2026: 5,326 delistings → 191 CEF/BDC filers → **151 fund deaths (141 CEF, 10 BDC)**. Outcomes: 79 liquidation/merger (N-8F dereg), 47 open-ending/conversion, 16 target-term maturities, **9 unknown**. **NAV-event 142/151 = 94%; ZERO distress delistings.** | **THE H6 SURVIVORSHIP GATE CLEARS ON DIRECTION.** The thesis's load-bearing claim is verified in-house: CEF deaths are overwhelmingly NAV events (liquidation/merger/open-end/term all pay ~NAV), so CEFConnect's current-only universe biases a discount-long backtest *against itself* — the project's #1 killer (missing dead names) is, uniquely here, a **tailwind**. With the prior Stage-1 census (185-fund tradable tail, 95% daily NAV, depth to ~2012 weekly), **H6 Stage-1 is materially complete and the decision gate now favours proceeding.** Residual before Stage-2: (a) confirm the 9 `delisted_unknown` + 10 BDCs are not distress via filing text; (b) extend the census to 2010–2020 (script is arg'd + fully cached/resumable — quick) for full-sample coverage; (c) BDC deaths can distress more than CEFs → Stage-2 universe should be CEF-only or flag BDCs. Fixes en route: EFTS 500s sporadically → retry/backoff added to the EDGAR fetcher; EFTS does not index N-2/N-8F as form facets (both 500) → those signals read from submissions JSON. Falsification gate re-passed (planted recovered / noise rejected); 10 new tests green (148 total; same 3 pre-existing env-only dashboard matplotlib `print_png(width=)` failures). Live config untouched. **NEXT: register H6 Stage-2 (the discount-z reversion trial) for owner sign-off → trial #11.** |

| 11 | 2026-06-15 | **trial** | H6: do small-fund CEF discounts mean-revert from z-extremes net of costs — the #1 graduation candidate, registered Stage-2 after Stage-1 cleared its gate (owner-greenlit "register and run Stage-2")? Run via the registration-gated `run_cef_reversion.py` after the synthetic machinery gate passed. | PIT current tradable tail (**179 funds**, mcap<$400M, dollar-ADV≥$250k, CEF-only), **weekly** discount-z = (discount−mean₅₂w)/std₅₂w from CEFConnect, dollar-neutral quintiles (LONG widest-discount / SHORT richest), 4-week rebalance, **weekly distribution-inclusive total-return** label (yfinance adj close), 25 bps/side, `--n-trials 11`. | OOS 858 weeks (2010→2026): net SR **1.11**, gross 1.98, **DSR 0.999**, IC **t_NW −10.4**, **skew +0.80**, maxDD −6%, turnover 11×/yr; beats EW-CEF (0.64); survives ex-largest-mcap-decile (1.09); daily-NAV-only control holds (1.10); shuffle 0.277 at registered seed. **All registered criteria nominally MET.** | **OVERTURNED by adversarial diagnostics — the first graduation that wasn't** (`results/h6_reversion_diagnostics.json`): (a) the **entry-lag sweep collapses SR 1.11→0.10 at a 1-week lag** and goes negative after — the entire "edge" is a single-week bounce, the signature of a microstructure / shared-price (bid-ask-bounce) reversal artifact, NOT multi-week reversion (cf. trial #8's graceful 0.87→0.39 decay = a real edge); the implementable 1-week-lag version is **~null (SR 0.10)**. (b) the shuffle control is **seed-fragile, breaching 0.3 on 3/6 seeds (max 0.52)** — the registered seed's 0.277 was luck. Mechanism: discount_w uses price_w and the next week's return divides by price_w, so noise in the measured close manufactures a one-week reversal you cannot trade (you'd buy at the very close you measured). **H6 does NOT graduate; NO criteria relaxation.** Sharp, citable lesson: the frozen criteria were INSUFFICIENT — a reversion strategy MUST register an entry-lag/implementability gate as pass/fail (trial #8 ran it only as a diagnostic; #11 proves why). The discipline caught a too-good result (prime directive: "if results look better, first hypothesis is a bug/leak") BEFORE it was claimed — the machine refusing its own false positive. The synthetic machinery gate passed first (planted_reversion recovered / random_walk rejected) → the harness is sound; the artifact lives in real-data microstructure, not the code. Stage-1's structural findings stand (conservative survivorship, tradable tail); the kill is the signal's IMPLEMENTABILITY. Falsification gate re-passed; N=11. |

| — | 2026-06-15 | infra | Execution/risk **ENGINE** started — the honest "scale it" machinery (turns a GRADUATED edge into geometric growth; NOT alpha). Owner pivot from a rejected "transition qr-alpha-lab into an aggressive alpha powerhouse" framing (no edge exists to scale; Kelly on a zero/negative edge maximizes ruin) to building the engine room honestly. | `src/quantlab/sizing.py` (fractional-Kelly-**under-uncertainty**: size on the LOWER confidence bound of the Sharpe via Lo-2002 SE + vol targeting → a near-zero/uncertain edge sizes to ~ZERO) + `src/quantlab/risk_model.py` (rolling market beta; factor-neutralization projection LᵀW→0; shrinkage covariance; ex-ante predicted vol; net-exposure report). 12 known-answer tests. Zero-trial; N=11. | Pure, source-agnostic, no data/trades — hands a target to execution. The honest reply to "maximize geometric growth": sizing a zero edge yields zero gross — the project's core lesson, in code. | Composes for the day a strategy graduates: signal → neutralize → vol-target → uncertainty-shrunk Kelly. Falsification gate re-passed; live config untouched. Next engine modules: multi-signal combiner, cost-aware Alpaca execution, drawdown/turnover/position limits. |

| — | 2026-06-15 | infra | H1 harness **PREP** (zero-trial) — build everything except the data so H1 is one command from trial #12 the day WRDS/CRSP access lands (sponsorship emails out to Wahal/Aragon + others). | `src/quantlab/fundamentals_data.py` (SEC XBRL **filing-date-PIT** layer: ticker→CIK, `companyconcept` parse keyed by `filed`, us-gaap tag fallbacks; `FundamentalsSource` interface with `FreeSECSource` [survivorship-UNSAFE] + `CompustatSource` adapter **SLOT**) + `src/quantlab/fundamentals.py` (GP/A & accruals/A features, quality signal z(GP/A)−z(accruals/A), quintile book, machinery gate) + `synthetic.make_quality_panel` (planted_quality/null_quality) + `scripts/run_fundamentals.py` (registration-gated). 7 known-answer tests + machinery gate. N=11. | LIVE SEC path verified (AAPL/MSFT/JPM/KO): ticker→CIK + filing-date GP/A & accruals/A assemble; **JPM correctly drops from GP/A — financials have no CoGS, the audit finding reproduced live**. Runner: registration gate ✓, machinery gate ✓ (planted SR≫0 / null ~0), then **DATA GATE REFUSES the free SEC source** (survivorship-blocked ~73% coverage) — no trial spent. | H1 is built, tested, gated — awaiting only a survivorship-safe source (swap `FreeSECSource`→`CompustatSource`, `--source compustat`). **Refinement before the graded run:** annualize flow fundamentals (TTM or 10-K-only GP/CoGS/NI/CFO) before dividing by point-in-time Assets — the free pull mixes 10-Q quarterly flows with stock Assets (understates e.g. MSFT GP/A); Compustat supplies clean annuals. Falsification gate re-passed; live trading config untouched. |

| — | 2026-06-15 | infra | Execution/risk **ENGINE completed** — the full signals→orders pipeline a multi-manager book runs (the honest "scale it" machinery: turns a GRADUATED edge into geometric growth, refuses to lever an ungraduated one). | New `src/quantlab/combine.py` (cross-sectional-z multi-signal combination + trailing-IC weighting), `limits.py` (position/gross/turnover caps + past-only drawdown de-grossing), `execution.py` (target weights → integer-share orders + turnover-cost; **PLANNING ONLY** — the frozen live Alpaca path is untouched), `engine.py` (`PortfolioEngine`: combine → neutralize [risk_model] → size [sizing: vol-target × confidence from the lower-bound Sharpe] → limits → execution; + `backtest` + `latest_orders`). 14 new known-answer/integration tests; composes the earlier sizing.py + risk_model.py. N=11. | **Honest-property integration (on the synthetic quality world): the engine commits to the planted edge (avg gross >0.2) and sizes the NULL book to ~ZERO (avg gross <0.05)** — the counterweight to H6 over-confidence; deliberately slow to lever (needs a confidently-positive trailing Sharpe over the lookback). Position/gross limits respected; `latest_orders` emits integer shares for the live client. | Engine is end-to-end: the day a strategy graduates (H1 via CRSP, or any future signal) it plugs straight into `PortfolioEngine` → neutralize → size → limit → orders. Pure (no data/trades); live trading config untouched. Falsification gate re-passed; 176 tests pass (3 pre-existing env-only dashboard matplotlib `print_png(width=)` failures, unrelated). |

| — | 2026-06-15 | infra | H1 free-data CIK recovery — can a historical ticker→CIK map close the survivorship hole the audit found, WITHOUT paid data? The follow-up the 2026-06-14 audit deferred. ZERO trials, no fundamentals pulled. | New `src/quantlab/cik_history.py` (EDGAR `getcompany` ticker→CIK resolver + reassignment-safety classifier: a hit is `plausible_dead` only if its last 10-K filing predates deletion+grace, else `possible_reassignment` = false link) + `scripts/h1_cik_coverage.py` → `results/h1_cik_coverage.json`; `tests/test_cik_history.py` (7 known-answer parser/classifier/injected-fetch tests). Name-based recovery (SEC `cik-lookup-data.txt`) is BLOCKED — our PIT universe (Wikipedia change table) carries only tickers, no company names. | 872 PIT tickers, **217 dead-AND-unmapped**. EDGAR resolved 52/217 — but only **20 `plausible_dead` (safe)**; **28 `possible_reassignment` (false-link risk)**, 4 unknown. Survivorship-safe coverage **73% → 75% (+2pp)**; residual hole **25%**. | **Free recovery is partial AND dangerous — the audit's conclusion, now MEASURED.** EDGAR's ticker index keeps only the recently-dead tail (ATVI/ABMD/ARNC, 2022–23); older M&A/bankruptcy names (AET, AGN, BHI, ABK, AKS…) are gone. Worse, **>½ of the hits are likely ticker REASSIGNMENTS** — a naive free map would silently bind a dead S&P name to a *living* company's fundamentals (a new survivorship-style leak the classifier caught before it could happen). Net: free data lifts safe coverage ~2pp; the bulk of the hole needs Compustat/CRSP — the empirical reinforcement of `run_fundamentals.py`'s DATA GATE. The reduced **accruals/A** variant (93% coverable on mapped names, sector-agnostic) stays registration-ready but inherits the SAME survivorship limit until the source swap. 189 tests green (7 new); falsification gate re-passed; N stays 11; live config untouched. |
| — | 2026-06-16 | infra | H1 free-SEC flow-annualization bug fix — GP/A and accruals/A were understating fundamentals by dividing mixed 10-Q flow numerators by point-in-time Assets. ZERO trials, no real-data H1 run. | `src/quantlab/fundamentals.py` now requests **annual-only** flow filings (10-K/10-K/A) for GrossProfit / Revenue / CoGS / NetIncome / CFO while leaving Assets point-in-time; `src/quantlab/fundamentals_data.py` caches filing metadata (`form`, `end`) so the free SEC source can filter annual-only rows; `tests/test_fundamentals.py` adds parser and panel known-answer regressions that fail if quarterly numerators sneak back in. | `python -m pytest -q` **191 passed** (workspace `TMP/TEMP` set locally because this Windows env cannot write to `C:\Users\galve\AppData\Local\Temp\pytest-of-galve`); falsification gate re-passed unchanged: planted net SR **0.8646**, DSR **0.9919** → PASS; noise DSR **0.0004** → rejected. | **Confirmed the bug lived in numerator frequency, not PIT alignment.** Annual-only flow numerators are the lowest-risk pre-data fix on the free SEC path: point-in-time safe, no invented TTM reconstruction from sparse metadata, and it blocks the quarterly-flow understatement until a richer As-Reported source (Compustat/Sharadar ARQ/ART) lands. N stays **11**; live config untouched. |

| — | 2026-06-17 | infra | PBO (Probability of Backtest Overfitting via CSCV, Bailey–López de Prado) added as the FAMILY-WISE complement to per-trial DSR, and applied to the equity config family. ZERO trials — a REPRODUCTION over already-logged configs #2/#3/#5/#6/#7 on the shared PIT return matrix (N unchanged). | New `src/quantlab/pbo.py` (CSCV: over a T×N matrix of COMPARABLE variants, P(in-sample-best ranks below the OOS median); + IS→OOS Sharpe degradation slope + P(OOS loss)) + `tests/test_pbo.py` (5 known-answer tests: noise family → PBO high, one-skilled-among-noise → PBO ~0, separation, guards). Applied via `scripts/pbo_equity.py` → `results/pbo_equity.json` over **3,253 aligned OOS days, 5 configs, 12,870 symmetric splits**. Scoped FAMILY-WISE on a shared matrix — NOT across the 11 heterogeneous trials (different universes/frequencies can't share a CSCV matrix; #1 biased-universe and #4 63d-horizon excluded on the record — that misuse is the very error the metric exists to catch). | **PBO = 0.24** — but read it WITH the companions: IS→OOS degradation slope **−0.89**, **P(OOS loss for the IS-best) = 0.71**, config net Sharpes all ≤ +0.07 (t2 +0.01, t7 +0.07, t6 −0.28, t3 −0.42, t5 −0.77). | **A LOW PBO here is NOT a green light — it is structural rank-persistence among uniformly-unprofitable configs.** The residual-label variants reliably underperform the raw-label one both IS and OOS, so selection stably picks the *least-bad* config (→ PBO < 0.5), but the −0.89 degradation slope and the 71% OOS-loss probability confirm the least-bad config still loses money net. PBO alone misleads on a family with no positive member; the honest read is PBO + degradation + OOS-loss together, and together they REAFFIRM the trials #2–7 equity null. The family-wise complement to DSR is now in the toolbox, pinned by tests. N stays 11; live config untouched; falsification gate re-passed. |
| — | 2026-06-24 | infra | Factor-neutral machinery + the two-world raw-vs-neutral DISCRIMINATOR for H1 (ZERO trials, N unchanged at 11): give H1 a pre-registered, validated way to tell a genuine quality alpha from value-collinear exposure. Designed + adversarially reviewed by two multi-agent workflows (17 design critiques + 6 post-impl findings, all independently re-verified); spec in `writeup/pbo_quality_harness_spec.md`. **Reconciled with the parallel 2026-06-17 PBO work on merge: ADOPTED the existing `pbo.cscv_pbo` for the four-arm CSCV adjudication rather than landing a duplicate CSCV module.** | ADDED: `risk_model.rolling_factor_betas` (past-only K-factor OLS, fit_intercept=True, reduces to `rolling_market_beta` at K=1 to 2.4e-15) + `loadings_at` + `cross_sectional_neutralize`; NEW `quantlab.ff_factors` (FF 5-factor monthly loader + daily stub); `synthetic.make_quality_panel` modes `quality_is_value` / `quality_orthogonal` (value-disguised vs value-orthogonal quality alpha, raw-SR-matched via `_WORLD_B_PREMIUM=0.004`); `fundamentals.value_neutralized_signal` (the NEUTRAL arm). The four-arm test drives `pbo.cscv_pbo`. | Two-world gate discriminates — World A NEUTRAL collapses (static-loading SR < 0.3), World B survives (> 1.0), paired diff > 1.0, and a placebo control proves the collapse needs the TRUE factor (neutral_true < 0.5×neutral_placebo); discrimination survives the production rolling-estimate loading path (neutral_B − neutral_A ≥ 1.58 across seeds 7/11/23, min at seed 7) and a poison-the-future leak check. H1 raw-vs-neutral (graduate on the NEUTRAL arm) + PBO ≤ 0.5 + the two-world gate are now pre-registered (preregistered_hypotheses.md H1 amendment 2026-06-24). **+20 new known-answer tests** (factor-betas 7, FF-loader 2 [skip on a fresh clone / CI where scratch_refute/F-F_Research_Data_5_Factors_2x3.csv is absent], quality-value 11). **`make_panel` untouched** → falsification gate byte-identical (planted SR 0.8646 / DSR 0.9919, noise DSR 0.0004); old quality modes byte-identical (`.equals()` 6/6). H1 still data-blocked; N stays 11. |

| — | 2026-06-24 | infra | CompustatSource adapter IMPLEMENTED (was a `NotImplementedError` slot) — H1's survivorship-safe data path. ZERO trials, N unchanged at 11; H1 still gated on the actual WRDS export landing. | `fundamentals_data.CompustatSource` now reads LOCAL WRDS/CRSP extracts (no live API): `field_series` (filing-date-PIT, Compustat-mnemonic mapping at→assets / ni→net_income / oancf→cfo / revt→revenue / cogs, gross_profit = `gp` else revt−cogs), `universe()` (survivorship-safe = union of all tickers ever in the extracts), `prices()` (delisting-inclusive, ffill to as-of), `start`/`end`. Documented WRDS recipe (funda + CRSP msf/msedelist + CCM link) in the class docstring. run_fundamentals.py `--source compustat` now passes the data gate and fails LOUDLY with the export recipe if extracts are absent (not on missing code). | 10 known-answer tests on synthetic WRDS-format fixtures: PIT-indexed mapping, gross_profit fallback, **REFUSAL to fall back to datadate/period-end (law #1 leak guard)**, annual-only filtering, dead-name retention, delisting NaN in prices, end-to-end through `fundamentals.pit_feature_panels`. Full suite green; falsification gate byte-identical (make_panel untouched). The harness is now ONE WRDS export from trial #12. |
| — | 2026-06-24 | infra | SEC↔Tiingo merge feasibility audit — does the free "SEC fundamentals + Tiingo prices" architecture clear H1's survivorship gate? ZERO trials, no forward returns. | New `src/quantlab/tiingo_data.py` (`TiingoSource`: survivorship-bias-free EOD via Tiingo, cached + rate-limited; `parse_eod_prices`/`parse_supported_tickers` known-answer tested, 4 tests) + `scripts/tiingo_sec_merge_audit.py` → `results/tiingo_sec_merge_audit.json`. Tiingo coverage measured via the BULK supported-tickers list (15,482 US equities incl. delisted) — a pure lookup, not per-name calls (the first design hit 429; the list IS the survivorship-safe universe + date-range crosswalk anchor). | **812 PIT S&P members ever, 503 current, 309 DEAD. Of the dead names: Tiingo prices 85%, SEC ticker→CIK crosswalk 39%, BOTH 39%.** Spot-check of 18 well-known acquired names (Celgene, Monsanto, Activision, Covidien, Baker Hughes, Cerner, Raytheon, Xilinx, Mylan, Tiffany, Allergan, Red Hat…): ALL present in Tiingo, NONE in the SEC current map except STI→CIK 1881551 — a **reassignment** (SunTrust merged into Truist 2019; the ticker now points at a recent registrant), the exact leak `cik_history` flagged. | **Tiingo SOLVES the price leg; the SEC dead-ticker→CIK crosswalk is a hard wall on free data — the "100% survivorship safe" claim does NOT survive contact with it.** End-to-end 39% (and some of that 39% are reassignments → truly-safe is lower) vs the ~95%+ a graded H1 needs. The truly-dead acquired names — what survivorship bias is ABOUT — are ~0% covered by free SEC fundamentals. Verdict: free SEC+Tiingo gives survivorship-safe PRICES but NOT survivorship-safe FUNDAMENTALS; the unlock is a historical/PIT ticker→CIK map (next free lever: Tiingo metadata `name` → SEC `cik-lookup-data.txt` name match, fuzzy + reassignment-gated) or paid Compustat/CRSP (the clean path, where `CompustatSource` is already built). N stays 11; live config untouched. |

| — | 2026-06-24 | infra | NAME→CIK crosswalk — the free survivorship UNLOCK. The Tiingo audit isolated the bottleneck to the dead-ticker→CIK *fundamentals* crosswalk (39%); this resolves dead names by COMPANY NAME instead of ticker. ZERO trials, N=11. Tested two rejected alternatives first: (a) user-proposed SEC bulk submissions.zip reverse-parse — REJECTED, the per-CIK `tickers` arrays are emptied at delisting (probed Celgene/Monsanto/Xilinx/Cerner/Red Hat: all `tickers=[]`), so it recovers ~0% for the same reason company_tickers.json does; (b) FMP `/stable/profile` cik — partial + reassignment-prone (covers Xilinx, misses Celgene, maps `MON`→a SPAC). | New `src/quantlab/cik_crosswalk.py`: `normalize_name` (drops /DE/-style qualifiers, leading "THE", trailing legal-form suffixes — NOT America/Trust etc., which would collide), `parse_cik_lookup` (SEC `cik-lookup-data.txt`, 1.05M names → normalized name→[CIK]), `match_name` (exact-normalized, else UNIQUE prefix — ambiguous prefix refused), `NameCikResolver.operating_cik` (latest-10-K filer among name matches; reassignment-immune by construction since it matches a NAME not a recycled ticker), `fetch_sp500_security_names` (dead names FREE from the Wikipedia "Removed Security" column — the piece prior CIK-recovery lacked). `scripts/h1_name_crosswalk_audit.py` → `results/h1_name_crosswalk_audit.json`. 5 known-answer tests. | **Dead-name fundamentals coverage 39% → 94% (name route), 95% either route** (309 dead names). Independently verified (not circular): of the 123 SURVIVORSHIP-CRITICAL names that truly stopped filing (acquired/bankrupt), recovered CIK name-consistent 113/123; **the 10 "inconsistent" hand-checked are ALL correct corporate-continuation lineages** (AK Steel→Cleveland-Cliffs, Sara Lee→Hillshire, Time Warner→WarnerMedia, MEMC→SunEdison, RadioShack→RS Legacy…) — the crosswalk correctly follows each firm through its acquisition/rename to the right CIK → ~100% correct on the dead subset. 5 famous names (CELG/MON/XLNX/CERN/RHT) pinned to ground-truth CIKs. | **Free SEC(name-crosswalk) + Tiingo PLAUSIBLY CLEARS the survivorship gate that has blocked H1 since trial #1** — ~94% dead-name fundamentals + 85% Tiingo prices → free 15-yr survivorship-safe H1 is in reach (no WRDS needed). Honesty caveats before a graded run: "dead" = removed-from-index (mix of truly-dead + alive-but-removed; both recovered correctly); the `dead_by` removal-date gate (built into `operating_cik`, not yet wired from the changes table) should tighten the alive-but-removed set and catch any rare live-namesake; a full spot-audit of all 290 recoveries precedes trial #12. `CompustatSource` remains the zero-gymnastics paid path. N stays 11; live config untouched. |

| — | 2026-06-24 | infra | Assembled the free survivorship-safe `FundamentalsSource` for H1 (machinery only, ZERO trials, N=11) — "one command from trial #12 on free data". Built in parallel (3-agent workflow) + full 290-recovery spot-audit. | NEW `src/quantlab/sec_xwalk_source.py` `SurvivorshipSafeSECSource` (survivorship_safe=True): resolves CIK **ticker-first** (current SEC map) then falls to the NAME crosswalk (`operating_cik(name, dead_by=index-removal-date)`) for genuinely-unmapped dead tickers; `field_series` reuses `FreeSECSource._concept_frame` semantics on the resolved CIK (filing-date PIT, annual-only flow filter); `universe()` = all PIT members; `prices()` = Tiingo (delisting-inclusive). Wired `run_fundamentals.py --source free_xwalk` (passes the DATA GATE; trial still needs explicit sign-off). 10 known-answer tests (fully offline, monkeypatched). | **Full spot-audit of all 290 name-route recoveries: 286/290 (98.6%) correct** (acquisition/rename continuations counted correct via SEC date-ranged formerNames). 2 namesake errors found — GHC (Graham Holdings→Graham Corp) and AAL (parent→subsidiary) — **both are LIVE-but-removed names that the assembled source resolves ticker-FIRST, so it dodges them by construction**; they only appeared in the name-ONLY audit. 2 watch (ADT/TYC→Johnson Controls lineage). | The free survivorship-safe path is now built end-to-end: SEC name-crosswalk fundamentals (~94% dead-name, ~99% correct) + Tiingo prices, behind the existing interface, ticker-first to avoid live-namesake collisions. Remaining before a graded trial #12 (deliberate N-spend, owner sign-off): wire `dead_by` from the changes table into the live path (built, the audit confirmed it's the right fix for the rare truly-dead namesake), then the full live pull + machinery gate + raw-vs-neutral run. `make_panel`/pipeline untouched. |

| — | 2026-06-24 | infra | H1 graded construction PRE-COMMIT REVIEW fixes — applied the review's NO-GO findings on the registered two-arm run (MACHINERY ONLY, ZERO trials, N stays 11). The headline (B3): the NEUTRAL arm is what H1 graduates on, and the runner graded it WITHOUT first proving in-env that neutralization can actually discriminate — the registration's amendment-2026-06-24 clause-C two-world gate was referenced in comments but NEVER RUN. | ADDED `fundamentals.neutralization_gate()` (+ `_static_loading_panel` helper): the four-clause in-env gate — value-DISGUISED world (`quality_is_value`) NEUTRAL must COLLAPSE (static-loading SR < 0.3), value-ORTHOGONAL world (`quality_orthogonal`) NEUTRAL must SURVIVE (> 1.0), the two worlds SR-MATCHED on the raw arm (mean raw-SR gap ≤ 1.0, so raw SR alone can't classify), and a PLACEBO control (collapse needs the TRUE value factor: neutral_true < 0.5×neutral_placebo). WIRED it into `run_h1_trial.main()` AFTER the machinery gate and BEFORE the DATA GATE, with sys.exit ABORT on failure (no trial spent). +4 known-answer tests (2 in `test_quality_value.py` pinning the gate per the registration + a no-op-neutralization guard that BITES; 2 in `test_run_h1_trial.py` pinning that main() invokes the gate before the DATA GATE and aborts on failure). | Gate PASSES today on the real machinery (is-value NEUTRAL SR [-0.08, -0.41, +0.25] all < 0.3; orthogonal NEUTRAL SR [2.44, 3.70, 2.93] all > 1.0; mean raw-SR gap 0.60; placebo control holds all seeds) — so the NEUTRAL verdict is now trustworthy. The no-op-neutralization guard confirms the gate is not a tautology (sabotaging `value_neutralized_signal` to a pass-through FAILS the gate). | **B3 re-verified and CLOSED: NEUTRAL can no longer silently collapse to RAW unproven.** The prior round already had the monthly price accessor (`prices_monthly`, daily→month-end NOT the quarterly grid) feeding past-only HML betas, the as-of alignment is leak-free (poison-the-future pins green), and the all-NaN-loading ABORT guard exists — this fix adds the missing *discrimination* proof the registration mandates as an ABORT condition. N stays 11; live config untouched; a graded trial #12 still requires owner sign-off + the Tiingo price-leg resume. |

## Notes

- 2026-06-24: **Trial #12 ATTEMPTED, not yet a result — N STAYS 11.** Owner
  authorized the graded H1 run on `--source free_xwalk`. The SEC fundamentals leg
  for all ~812 PIT survivorship-safe names PULLED + CACHED successfully (the novel
  unlock works end-to-end, pre-flight verified: CELG/MON/XLNX/RHT resolve to the
  right CIK with filing-date-PIT fundamentals ending exactly at acquisition). The
  PRICE leg is blocked: Tiingo's daily quota was exhausted (a guaranteed-covered
  name, AAPL, returns 0 rows = rate-limited, not absent), so the run produced no
  backtest. No valid result → N unchanged (a trial increments N only on a logged
  result). `tiingo_data` hardened to be rate-limit-RESILIENT + RESUMABLE (slower
  pacing, harder 429 backoff, skip-without-caching so a re-run fills gaps). RESUME
  after the Tiingo quota resets, via the REGISTERED entry point: `python
  scripts/run_h1_trial.py --source free_xwalk --hypothesis H1 --n-trials 12` (the
  graded two-arm RAW-vs-NEUTRAL CBOP/A construction; `run_fundamentals.py` is a
  thin alias that delegates to the same registered run — the old GP/A-blend
  placeholder it once held is neutralized). SEC is cached; only Tiingo prices
  refetch. Did NOT fall back to yfinance prices — that would re-inject
  survivorship bias on the price side and void the trial's entire point.
- 2026-06-10: N = 2 (trial #1 biased universe, trial #2 PIT universe).
  The Newey–West caveat from trial #1 is now fixed; all t-stats quoted are NW.
- Phase 2 AND Phase 3 complete: real data, CI falsification gate, PIT universe
  with quantified residual bias, sector/beta neutralization with measured (not
  asserted) exposures. Headline finding: the default 5-feature ridge has NO
  defensible edge on the honest universe, raw or neutralized (trials #2, #3).
- Alpaca paper account connected and verified (read-only check; $1M paper equity).
- Phase 4 agenda (every variant = +1 trial): residualized-return labels,
  multiple horizons (5/21/63d), z-score features over members only, turnover
  reduction (7.4×/yr is the enemy of any weak edge at 10 bps), feature
  importance stability across walk-forward windows.
- 2026-06-10 (Phase 7 started): full prose draft of the research note in
  `writeup/research_note_draft.md` (owner to rewrite in his voice — every
  number traced to a log row or results/ artifact). New
  `writeup/preregistered_hypotheses.md`: H1 fundamentals (blocked on a
  point-in-time data audit), H2 crypto-perp carry, H3 regime-gated
  momentum (registered with a trap check). Nothing run; N stays 7; each
  future run needs owner sign-off and increments N by exactly 1.
- 2026-06-10 (PC migration): work moved laptop → PC mid-Phase-6. The copy
  arrived as a GitHub ZIP (no `.git`, no `.env`), so this log entry for the
  Phase 6 deployment was reconstructed from the laptop session summary and
  must be reconciled against the laptop/GitHub copy of this file when the
  repo is re-cloned. Environment re-verified on PC: fresh venv, 49 tests
  green, falsification gate re-passed with planted/noise numbers matching
  the logged baseline to reported precision (IC 0.0629 / net SR 0.8646 /
  DSR 0.9919; noise rejected). Cross-platform float drift is ~1e-14
  (different CPU/BLAS); the laptop's canonical results/ artifacts were kept
  rather than committing no-information churn.
